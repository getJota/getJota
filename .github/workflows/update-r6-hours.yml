name: Update R6 Siege Hours

on:
  schedule:
    - cron: '0 6 * * *'   # Corre todos los días a las 06:00 UTC
  workflow_dispatch:

jobs:
  update-hours:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v5

      - name: Install jq
        run: sudo apt-get update && sudo apt-get install -y jq

      - name: Fetch playtime from Steam and update README
        env:
          STEAM_API_KEY: ${{ secrets.STEAM_API_KEY }}
          STEAM_ID: ${{ secrets.STEAM_ID }}
        run: |
          set -euo pipefail
          APPID=359550

          echo "📡 Consultando API de Steam..."
          URL="https://api.steampowered.com/IPlayerService/GetOwnedGames/v1/?key=${STEAM_API_KEY}&steamid=${STEAM_ID}&include_appinfo=false&include_played_free_games=true&appids_filter[0]=${APPID}"
          RESP=$(curl -s "$URL")
          echo "✅ Respuesta de la API:"
          echo "$RESP" | jq .

          MINUTES=$(echo "$RESP" | jq -r '.response.games[0].playtime_forever // 0')
          if ! echo "$MINUTES" | grep -Eq '^[0-9]+$'; then
            echo "⚠️ No se encontraron minutos válidos, usando 0."
            MINUTES=0
          fi

          HOURS=$(( (MINUTES + 30) / 60 ))
          DATE=$(date -u +%F)

          echo "⏱️ Minutos totales: $MINUTES"
          echo "🕒 Horas (redondeadas): $HOURS"
          echo "📅 Fecha: $DATE"

          # Crear el JSON de estadísticas
          printf '{ "hours": %d, "minutes": %d, "last_updated": "%s" }\n' "$HOURS" "$MINUTES" "$DATE" > r6_stats.json
          echo "✅ Archivo r6_stats.json generado:"
          cat r6_stats.json

          # Asegurarse de que las etiquetas existen en README
          if ! grep -q '<span id="r6-hours">' README.md; then
            echo -e "\n**Total (aprox):** <span id=\"r6-hours\">`--` horas</span>\n_Last updated: <span id=\"r6-last\">`--`</span>._\n" >> README.md
          fi

          # Actualizar README usando Perl
          perl -i -pe 's|<span id="r6-hours">.*?</span>|<span id="r6-hours">`'"$HOURS"'` horas</span>|g;
                       s|<span id="r6-last">.*?</span>|<span id="r6-last">`'"$DATE"'`</span>|g' README.md

          echo "✅ README actualizado."

          # Configurar git
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"

          # Commit y push solo si hay cambios
          git add README.md r6_stats.json
          if ! git diff --cached --quiet; then
            git commit -m "chore: actualizar horas de R6 Siege"
            git push origin main
          else
            echo "No hay cambios para commitear."
          fi
